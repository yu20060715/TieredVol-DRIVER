/* Minimal mocks of misc kernel headers needed by tieredvol.h.
 * Only type shapes required for the feasibility prototype. */
#ifndef _MOCK_LINUX_KTIME_H
#define _MOCK_LINUX_KTIME_H

#include "types.h"

typedef u64 ktime_t;

#endif
